# Aries OS — Progreso

> Fuente de verdad del estado del proyecto. Se actualiza al TERMINAR cada tarea, no al empezarla.
> Antes de cualquier tarea nueva, leer este archivo primero.

## Investigación de los 2 commits inesperados (Tarea 0, sesión nocturna 2026-07-24, segunda noche)

Comparé línea por línea `git show adb9429 --stat` y `git show 2fb87be --stat` contra el historial de tareas de este archivo y contra `docs/audits/2026-07-24-pre-commit-review.md`.

**Conclusión: el contenido de ambos commits coincide 100% con trabajo ya documentado. No hay ninguna línea sin explicación.**

- `adb9429` ("Completa refactor de Event Bus"): los 8 archivos que toca (`docs/contracts/IEventBus.md`, `__main__.py`, `contracts/__init__.py`, `core/events.py`, `events/__init__.py`, `events/publisher.py`, `tests/conftest.py`, `contracts/event_bus.py`) son exactamente los que `docs/audits/2026-07-24-pre-commit-review.md` ya identificó: los primeros 7 preexistentes de una sesión previa no comiteada, y `contracts/event_bus.py` incluyendo además mi fix de import circular (documentado en el addendum de `docs/audits/2026-07-24-diagnostico.md`). El propio mensaje del commit ya describe esto correctamente — coincide palabra por palabra con esa conclusión.
- `2fb87be` ("memory/: expires_at + clear_expired; FileSystemAgent y ProcessAgent"): los 20 archivos corresponden 1:1 a las tareas de `memory/`/`FileSystemAgent`/`ProcessAgent` ya descritas abajo en "Qué existe implementado", incluyendo las pruebas preexistentes de `test_kernel.py` (`test_kernel_publishes_initialized_event`/`_shutdown_event`, documentadas como preexistentes en "Baseline conocido") mezcladas con mi única línea nueva en `core/kernel.py` (`memory.store(...)`).

Lo que **no pude confirmar** es el mecanismo: no hay ningún `git add`/`git commit` en mi historial de tool calls de la sesión anterior, ni hooks de git customizados en `.git/hooks/` (solo los `.sample` por defecto). El contenido no es sospechoso — coincide 100% —, pero el *cómo* se generaron estos commits sigue sin explicación (sospecha, no confirmada: algún mecanismo de checkpoint del entorno/extensión VS Code, dado que el contexto de sesión indica "VSCode native extension environment"). No cumple la condición que la tarea de esta noche definió para el flag en mayúsculas ("si hay CUALQUIER línea que no coincida con ninguna tarea conocida") — sí coincide todo —, así que no lo marco como `[SIN RESOLVER]`, pero lo dejo acá arriba, visible, para que se confirme el mecanismo mañana. **No se ejecutó ningún `git commit`/`git push` en esta sesión** (instrucción explícita: nunca comitear/pushear).

## Hallazgo sin resolver: regresión en `events/event_bus.py` (tercera noche, no causada por esta tarea)

Al leer `events/event_bus.py` como referencia de estilo para conectar `plugins/` al Event Bus real, encontré que **volvió a definir una clase `EventBus(ABC)` local** (con `AsyncEventBus(EventBus)` heredando de ella), revirtiendo el fix del ciclo de import de hace dos noches (que hacía `AsyncEventBus` heredar de `contracts.event_bus.IEventBus`, sin clase local duplicada). Verificado con `git diff HEAD -- src/aries/events/event_bus.py`: es el **único** archivo del cluster de eventos con diferencia contra el último commit — `subscriber.py`, `events/__init__.py` y `contracts/event_bus.py` siguen exactamente como quedaron comiteados (con mi fix intacto). Yo no toqué este archivo en ninguna tarea de esta sesión ni de la anterior.

**Impacto verificado, no asumido:** `import aries` sigue funcionando (`python -c "import aries"` ok), pero `isinstance(AsyncEventBus(), IEventBus)` ahora da `False` — `AsyncEventBus` sigue siendo compatible por duck-typing (mismos métodos `publish`/`subscribe`/`unsubscribe`) pero dejó de ser formalmente un `IEventBus` del contrato. No rompe nada de lo que hasta ahora se probó (los 289 tests de la suite completa siguen pasando), pero es una inconsistencia real de tipos.

**No lo corregí** — no es parte de esta tarea (que pidió `plugins/`, no tocar `events/`) y no está bloqueando nada. Para `plugins/`, seguí tipando contra `aries.contracts.event_bus.IEventBus` (la convención ya establecida en `core/kernel.py` y `events/publisher.py`, ambos intactos), no contra la clase local duplicada de `event_bus.py`. Revisar mañana junto con el hallazgo de los commits de la noche anterior.

## Estado actual (fecha de hoy)
| Fase | Estado |
| --- | --- |
| v0.1 Blueprint | completa |
| v0.2 Foundation | completa |
| v0.3 Kernel | parcial |
| v0.4 Planner | no iniciada |
| v0.5 Plugins | implementada (manifest/loader/registry/context/installer real; falta instalación real de deps, deliberadamente) |
| v0.6 Memory | parcial |
| Agents | parcial (4 agentes + AgentManager: FileSystemAgent, ProcessAgent, GitAgent, DatabaseAgent) |
| v0.7 Voice | no iniciada |
| v1.0 MVP | no iniciada |

## Baseline conocido
- `pytest` tiene 2 fallos intermitentes esperados (no son regresiones nuevas): `tests/unit/test_kernel.py::test_kernel_publishes_initialized_event` y `::test_kernel_publishes_shutdown_event` — detalle en "Qué NO existe todavía".

## Qué existe implementado (código real, no contratos)
- `src/aries/core/kernel.py`: kernel básico con initialize/run/shutdown y estado local.
- `src/aries/config/settings.py`: configuración de aplicación con Pydantic Settings y .env loading.
- `src/aries/logging/__init__.py`: inicializa structlog con renderer y logger.
- `src/aries/api.py`: FastAPI app mínima con endpoint `/health`.
- `src/aries/exceptions/__init__.py`: excepciones personalizadas definidas.
- `src/aries/types.py`: alias de tipos comunes.
- `src/aries/memory/in_memory.py`: implementación de IMemory en memoria local.
- `src/aries/llm/ollama_provider.py`: proveedor Ollama con AsyncClient, complete, embed e is_available.
- `src/aries/core/kernel.py`: kernel inyecta IMemory e ILLMProvider y valida disponibilidad de LLM al iniciar.
- `src/aries/__main__.py`: construcción de Kernel con InMemoryStore y OllamaProvider usando async context manager.
- `tests/unit/test_kernel.py`: tests de kernel con fake ILLMProvider y disponibilidad configurable.
- `tests/unit/test_config.py`: tests de carga de configuración y overrides de env.
- `src/aries/contracts/memory.py` y `src/aries/memory/in_memory.py`: se agregó el campo `expires_at` a `MemoryItem` y un parámetro opcional `expires_at` a `store()`; `clear_expired()` ahora borra de verdad los items vencidos (antes era un no-op permanente). `core/kernel.py` ahora usa `self.memory.store(...)` en `initialize()` para guardar un item tipo `context` — primer uso real de Memory en el flujo del kernel. Detalle completo en `docs/audits/2026-07-24-diagnostico.md`.
- Se corrigió un import circular preexistente entre `src/aries/contracts/event_bus.py` y `src/aries/events/__init__.py` que impedía `import aries` por completo (bloqueaba correr cualquier test). Fix mínimo de orden de imports, sin cambios de lógica de negocio en `events/`. Ver addendum en `docs/audits/2026-07-24-diagnostico.md`.
- `src/aries/agents/filesystem/agent.py`: **primer `IAgent` concreto del proyecto.** `FileSystemAgent` implementa `execute`/`get_capabilities`/`requires_confirmation`/`is_available`/`get_agent_name` con `pathlib` puro (sin dependencias nuevas). Capacidades: `open_file`/`read_file` (alias), `list_directory`, `create_directory`, `delete_file` (única acción con `requires_confirmation() == True`), `create_file`/`write_file` (alias; `create_file` no sobrescribe por defecto, `write_file` sí). `execute()` atrapa `FileNotFoundError`/`PermissionError`/`FileExistsError`/`IsADirectoryError`/`NotADirectoryError`/`OSError` y los devuelve como `ActionResult(status=FAILED, error=...)` en vez de dejarlos propagar — esto ya quedó resuelto formalmente en `docs/contracts/IAgent.md` y en el docstring de `contracts/agent.py::execute()` (antes decían `Raises: PermissionError`, ahora dicen explícitamente que nunca se propagan excepciones). 35 tests en `tests/unit/test_filesystem_agent.py`, incluyendo archivo/directorio inexistente y permiso denegado (archivos de solo lectura vía `os.chmod`, y lectura de un directorio como archivo — en Windows eso levanta `PermissionError`, no `IsADirectoryError`).
- `src/aries/agents/process/agent.py`: **segundo `IAgent` concreto.** `ProcessAgent` ejecuta comandos/scripts y gestiona procesos con `subprocess`/`os`/`signal` puros (sin `psutil`, que no está en `pyproject.toml` — se resolvió `list_processes`/`get_process_info` parseando la salida CSV de `tasklist`, utilidad de consola exclusiva de Windows). Capacidades: `run_command` (**shell=False**, tokeniza el string de comando con `shlex.split()` — ver corrección de seguridad abajo —, captura stdout/stderr/exit_code, timeout configurable, default `DEFAULT_TIMEOUT_SECONDS = 30.0`), `run_script` (shell=False, argv explícito; intérprete inferido por extensión — `.py`→`sys.executable`, `.ps1`→PowerShell con `-ExecutionPolicy Bypass`, `.sh`→`bash` — o explícito vía kwarg `interpreter`), `kill_process` (`os.kill(pid, SIGTERM)`, que en Windows sí mapea a `TerminateProcess`), `list_processes`, `get_process_info`. `requires_confirmation()` extiende la firma del contrato con un kwarg opcional `command: str | None = None` (retrocompatible, y ya documentado formalmente en `docs/contracts/IAgent.md`) para poder evaluar el contenido del comando: devuelve `True` siempre para `kill_process`, y para `run_command` solo si el primer token de algún segmento del comando (separado por `;`/`&`/`|`) coincide con una lista corta de nombres destructivos conocidos (`rm`, `del`, `format`, etc.) — heurística de mejor esfuerzo, no un sandbox. `os.kill` sobre un PID inexistente en Windows lanza `OSError` genérico (`WinError 87`), no `ProcessLookupError`, por eso `kill_process`/`get_process_info` verifican existencia vía `tasklist` primero y levantan `ProcessLookupError` ellos mismos. 41 tests en `tests/unit/test_process_agent.py`, incluyendo timeout (`run_command` y `run_script`), comando/script/PID inexistente, comillas mal formadas, comando vacío, permiso denegado en `kill_process` simulado con `monkeypatch` (deliberadamente no se testeó matando un PID protegido real del sistema), una prueba de regresión de seguridad que confirma que `&&` ya no encadena un segundo comando, y una prueba de regresión que confirma que una ruta Windows sin comillas ya no se corrompe.
- **Corrección de seguridad en `run_command`:** originalmente usaba `shell=True` con el string de comando tal cual. Se cambió a tokenizar con `shlex` + `shell=False`, cerrando el vector de inyección de comandos (`;`, `&&`, `|` dejan de ser operadores de shell y se pasan como argv literal — hay un test de regresión que lo confirma). **Costo funcional real, no una elección mía:** se pierde soporte para pipes, redirects (`>`/`<`), expansión de variables (`%VAR%`).
- **Aislamiento Windows-específico en `ProcessAgent`:** `list_processes`/`get_process_info`/la verificación de existencia de `kill_process` quedaron detrás de `_list_processes_windows()`/`_get_process_info_windows()`, con un único punto de detección de SO (`_require_windows()`, vía `platform.system()`). Los handlers públicos no saben que son Windows-específicos. Linux/Mac levantan `NotImplementedError` desde ese único punto — no implementados todavía, ver `execute()` que ahora también atrapa `NotImplementedError` y lo retorna como `ActionResult(FAILED)`, consistente con "nunca propagar excepciones".
- **Corrección del tokenizador de `run_command` (`shlex.split(..., posix=False)` + limpieza manual de comillas, en `_split_command()`):** `shlex.split()` en modo POSIX (el default) trata `\` como carácter de escape, corrompiendo rutas Windows **sin comillas** (`C:\Windows\System32\app.exe` → `C:WindowsSystem32app.exe`, verificado empíricamente). `posix=False` preserva las barras invertidas, pero como efecto secundario deja las comillas de agrupación como caracteres literales en el token — así que `_split_command()` las remueve a mano después de tokenizar. Con esto, tanto rutas sin comillas como argumentos entre comillas (`-c "código"`) funcionan correctamente; hay test para ambos casos.
- **Decisión sobre comandos internos de cmd.exe — cerrada, no queda pendiente:** `dir`, `cd`, `echo`, `type`, `copy`, `del`, `set`, `cls`, etc. no tienen `.exe` propio en Windows (solo existen dentro de un shell), así que `run_command(command="dir")` falla con `ActionResult(FAILED)` — y **se deja así a propósito**, no se agrega un prefijo automático `["cmd", "/c", ...]` que reintroduzca superficie de shell. Razón: esa funcionalidad ya existe donde corresponde — listar/crear/borrar archivos es `FileSystemAgent` (`list_directory`, `delete_file`, etc.), y cambiar de directorio para un comando es el parámetro `cwd` que `run_command`/`run_script` ya aceptan. `ProcessAgent` es deliberadamente solo para ejecutables reales, no para reimplementar un shell.

### Sesión nocturna autónoma — GitAgent (tercer IAgent concreto)

- `src/aries/agents/git/agent.py`: **tercer `IAgent` concreto.** `GitAgent` ejecuta operaciones Git invocando el binario `git` vía `subprocess`, siempre `shell=False` con argv explícito (ninguna capacidad interpola texto libre en un comando de shell — no hay un `run_git_command(command: str)` genérico tipo `ProcessAgent.run_command`). Capacidades: `status`, `add` (archivos específicos o todo vía `git add -A`), `commit`, `push`, `pull`, `log` (últimos N commits, parseados con un separador `\x1f` para evitar ambigüedad con mensajes que contengan el delimitador obvio), `diff` (working tree o `--staged`), `branch_list`, `branch_create` (con `checkout` opcional), `branch_checkout`, `reset` (`mode="soft"|"hard"`, default `"soft"`). `requires_confirmation()` usa el mismo patrón de extensión ya documentado en `docs/contracts/IAgent.md` (kwargs opcionales además de `action`): `True` para `push(force=True)` y `reset(mode="hard")`, que son exactamente los dos casos que la tarea pidió explícitamente — con el set de capacidades actual no hay otra operación que reescriba historia (no hay `rebase`/`commit --amend`/`filter-branch`).
- **Diferencia deliberada de filosofía vs. `ProcessAgent`:** en `GitAgent`, `ActionResult.status` refleja el resultado semántico real de la operación de Git (`exit_code != 0` → `FAILED`), no solo si el proceso pudo arrancar. Es lo opuesto a `ProcessAgent.run_command`/`run_script`, donde `SUCCESS` significa "el shell/intérprete corrió" sin importar el `exit_code` del programa. Tiene sentido acá porque cada capacidad de `GitAgent` es una operación con significado propio (`commit` "tuvo éxito" o no), a diferencia de `run_command` que ejecuta cualquier cosa.
- **Sin capa de aislamiento de SO** (a diferencia de `ProcessAgent`/`_require_windows()`): `git` es el mismo binario con el mismo comportamiento en Windows/Linux/Mac, no hay ninguna llamada SO-específica que aislar. Documentado explícitamente en el docstring del módulo para que quede claro que es una omisión deliberada, no un olvido.
- **Hallazgo empírico no obvio, verificado antes de escribir el código (no asumido):** Git no es consistente sobre a qué stream de salida manda sus mensajes informativos — `checkout`/`push` escriben su resumen a **stderr** incluso en éxito (exit 0); `reset --hard`/`pull` usan **stdout**. `execute()` arma `output` con fallback `stdout or stderr` para cubrir ambos casos. También: en Windows, `subprocess.run(cwd=<algo>)` levanta `NotADirectoryError` tanto si el path no existe como si existe pero es un archivo — nunca `FileNotFoundError` para esos casos (sí para cuando no se encuentra el binario `git` en sí). El mensaje de error de `repo_path` se redactó para cubrir ambos casos.
- **Alcance de `reset` deliberadamente acotado a `soft`/`hard`**, sin `mixed` — la tarea solo mencionó esos dos modos explícitamente; no se amplió sin que se pidiera. Default `mode="soft"` (el menos destructivo de los dos soportados) por ser la sesión nocturna sin supervisión: ante ambigüedad, la opción más conservadora.
- 49 tests en `tests/unit/test_git_agent.py`, **ninguno mockea `git`** — todos usan repos reales creados con `git init`/`git init --bare` sobre directorios temporales (mismo criterio que `FileSystemAgent`/`ProcessAgent`), incluyendo push/pull contra un repo bare real, reset soft vs. hard verificando el estado real del working tree, y los casos de error (repo inexistente, no es un repo git, timeout, mensaje de commit vacío, nada para commitear, rama inexistente).
- `docs/specs/Planner.spec.md`: **borrador de spec, no código** — marcado "BORRADOR — requiere aprobación" al inicio. Cubre qué recibe el Planner como input, cómo decidiría entre Agent y Tool, la secuencia de eventos que publicaría (usando el catálogo de `docs/audits/2026-07-24-diagnostico.md`), y la discrepancia real entre `ActionResult`/`ToolResult` que el Planner tendría que resolver. Termina con una lista de 9 puntos `[REQUIERE DECISIÓN]` explícitamente sin resolver (ej. prioridad Tool-vs-Agent, quién le da input al Planner en primer lugar — `Kernel.run()` sigue siendo un stub —, cómo se normalizan `ActionResult` vs `ToolResult`). Revisar antes de implementar Planner de verdad.

### Sesión nocturna autónoma (segunda noche) — DatabaseAgent (cuarto IAgent concreto)

- **Nota de entorno, antes del código:** `sqlalchemy>=2.0` está en `pyproject.toml` pero **no estaba instalada** en el entorno que se viene usando toda la sesión (Python global — confirmé que ni `.venv` ni `.venv-1` son el entorno real: `.venv` está vacío salvo `pip`, `.venv-1` solo tiene tooling de lint/test — mypy/pytest/coverage —, no las dependencias de runtime del proyecto). La instalé (`pip install "sqlalchemy>=2.0"`) en el entorno global antes de escribir código, porque ya estaba "aprobada" en el manifiesto — no es una dependencia nueva, es completar lo que el proyecto ya declaraba. Documentado acá por transparencia, no pedí confirmación (instrucción de la noche: no bloquear).
- `src/aries/agents/database/agent.py`: **cuarto `IAgent` concreto.** `DatabaseAgent` ejecuta SQL sobre SQLite usando SQLAlchemy **Core** (no ORM) — `execute_query` (parametrizado, `query` + `params` separados, nunca interpolación de string), `insert`/`update`/`delete` (vía reflexión de tabla + SQLAlchemy Core, sin armar SQL a mano en ningún punto — protege tanto valores como nombres de columna/tabla), `list_tables`, `get_schema`, `execute_transaction` (atómica de verdad, un solo `engine.begin()` para toda la lista de operaciones). Alcance acotado a `db_path` de archivo SQLite (sin connection strings arbitrarios ni otros motores — no se pidió explícitamente, no se amplió el alcance).
- **Inyección SQL tratada con el mismo criterio que `shell=True` en `ProcessAgent`:** verifiqué empíricamente (no asumí) tres vectores antes de escribir el código: (1) inyección vía valor de parámetro en `execute_query` — queda como literal, no se ejecuta; (2) inyección vía "nombre de columna" malicioso en `values`/`where` de `insert`/`update` — SQLAlchemy lo rechaza con `CompileError`/`KeyError` antes de tocar la base; (3) confirmé que un valor legítimo puede contener sintaxis SQL arbitraria (comillas, `;`, `--`) y se guarda tal cual como dato, sin ejecutarse. Los 3 casos tienen test dedicado en `TestSQLInjection`, verificando el estado real de la tabla después del intento, no solo el `ActionResult`.
- **`requires_confirmation`** extiende la firma con `query`/`where`/`operations` opcionales (mismo patrón ya documentado en `docs/contracts/IAgent.md`): `True` para SQL crudo que empiece con `DROP TABLE`/`TRUNCATE`, o `DELETE`/`UPDATE` sin `WHERE` (heurística de texto, mismo espíritu que `ProcessAgent._looks_destructive` — no es un control de seguridad, la seguridad real es la parametrización); `True` para `update`/`delete` sin `where`; y recursivamente `True` para `execute_transaction` si cualquiera de sus operaciones lo dispara. Igual que en `GitAgent`/`FileSystemAgent`, `execute()` no se autobloquea con esto — es responsabilidad de quien llama pedir confirmación antes de invocar (verificado con test explícito: `update`/`delete` sin `where` sí se ejecutan cuando se llaman directamente).
- **Sin capa de aislamiento de SO** (igual que `GitAgent`): SQLAlchemy/SQLite ya son portables.
- 48 tests en `tests/unit/test_database_agent.py`, **ningún mock del motor** — todos contra archivos SQLite reales en disco (`sqlite3` stdlib para el fixture de setup, el agente para todo lo demás), incluyendo transacción atómica con rollback real verificado (una operación intermedia fallida no deja cambios de las anteriores), violación de constraint UNIQUE, tabla/columna inexistente, y los 3 casos de inyección ya descritos arriba.

### Sesión nocturna autónoma (tercera noche) — AgentManager conecta los 4 agentes por primera vez

- `src/aries/agents/manager.py`: **implementado, deja de ser un stub vacío.** `AgentManager` registra explícitamente los 4 `IAgent` concretos (`FileSystemAgent`, `ProcessAgent`, `GitAgent`, `DatabaseAgent`) al construirse — **sin descubrimiento automático**, a propósito (eso es trabajo de `plugins/`, 0% implementado, fuera de alcance). `dispatch(agent_name, action, **kwargs)` valida que el agente exista y que `action` esté en `get_capabilities()` antes de llamar a `execute()`; si algo no existe, retorna `ActionResult(status=FAILED, error=...)` sin propagar excepciones — mismo criterio que todo `IAgent.execute()`. El `ActionResult` de un agente real se retorna **tal cual**, sin transformarlo (verificado con un test que compara `dispatch()` contra una llamada directa a `execute()` con los mismos parámetros y confirma igualdad). `list_agents()` expone `{nombre_agente: [capacidades]}` para cuando exista Planner. `register()`/`get_agent()` permiten agentes adicionales (usados en los tests con un `FakeAgent`, sin tocar los 4 reales).
- **No encontré ningún bug de contrato al integrar los 4 agentes** — verifiqué `get_agent_name()`/`get_capabilities()` de los cuatro contra el código real antes de escribir los tests (no contra memoria): `"filesystem"`, `"process"`, `"git"`, `"database"`, sin colisiones de nombre ni inconsistencias. No hizo falta tocar ninguno de los 4 agentes.
- 16 tests en `tests/unit/test_agent_manager.py`: registro/listado, agente inexistente, acción inexistente (incluyendo uno que confirma que `dispatch()` ni siquiera llama a `execute()` si la acción no está declarada), y **un test de integración end-to-end real por cada uno de los 4 agentes** (`FileSystemAgent` escribiendo/leyendo un archivo real, `ProcessAgent` corriendo un proceso Python real, `GitAgent` contra un repo git real, `DatabaseAgent` contra un SQLite real) — ninguno mockeado, todos vía `AgentManager.dispatch()`.
- `agents/base.py` sigue vacío — la tarea era específicamente `manager.py`, no se tocó.

### Sesión nocturna autónoma (cuarta noche) — sistema de Plugins completo (`plugins/` pasa de 0% a implementado)

- **`src/aries/plugins/manifest.py`**: `parse_manifest(path)` lee y valida un `manifest.json` (formato elegido a propósito sobre un `manifest.py` con dict Python — leer metadata de un plugin no debe requerir ejecutar código del plugin) contra los campos de `PluginMetadata`: `name` debe ser minúsculas/sin espacios, `version` semántica (`\d+\.\d+\.\d+`), `entry_point` con formato `"modulo:Clase"` (estilo entry points de setuptools), `requires` lista de strings. Cualquier problema (archivo faltante, JSON malformado, campo faltante/inválido) se normaliza en una única excepción `ManifestError` con mensaje claro — nunca deja escapar `OSError`/`JSONDecodeError`/`KeyError` crudos.
- **`src/aries/plugins/loader.py`**: `load_plugin(plugin_dir)` parsea el manifest, importa el módulo de `entry_point` vía `importlib.util.spec_from_file_location` (con un nombre de módulo único por plugin en `sys.modules`, para que dos plugins con un archivo `plugin.py` homónimo no choquen) y **instancia** la clase — **sin llamar a `initialize()`**, tal como pidió la tarea (eso es responsabilidad de `PluginRegistry`). Cualquier falla de import/clase-inexistente/clase-que-no-implementa-`IPlugin`/excepción-al-instanciar se normaliza en `LoaderError`.
- **`src/aries/plugins/context.py`**: `build_plugin_context(event_bus, plugin_name, settings)` arma el dict que `initialize()` recibe: `logger` real (con nombre `plugin.<nombre>`), `event_bus` real (la misma instancia inyectada en `PluginRegistry`, nunca un mock), `settings` (lo que el caller pase), y **`di_container=None`, documentado a propósito** — no existe ningún contenedor de inyección de dependencias real en el proyecto (`container/` fue eliminado explícitamente, confirmado con `grep` que no hay `DIContainer` en ningún lado de `src/`) y no se inventó uno nuevo para esta tarea, tal como se pidió.
- **`src/aries/plugins/registry.py` — `PluginRegistry`**: `load(plugin_dir)` hace manifest → import/instancia → **`check_requirements()` (bloquea el load si falta algo, no solo advierte — decisión conservadora explícita, ver abajo)** → `initialize(context)` → `register_hooks()` → conecta cada hook al **Event Bus real** (`IEventBus` de `contracts/event_bus.py`, no un mock) → publica `PluginLoadedEvent`. `unload(name)` desconecta hooks, llama `shutdown()`, remueve del registro, publica `PluginUnloadedEvent`. Ambos devuelven `(bool, str | None)` y **nunca propagan excepciones** (cada paso tiene su propio catch, más una red de seguridad final envolviendo todo el método) — mismo criterio que `IAgent.execute()`/`AgentManager.dispatch()` en el resto del proyecto. Si `register_hooks()` falla después de que `initialize()` ya corrió, se hace rollback llamando `shutdown()` antes de reportar el error (verificado con test dedicado, incluyendo que `shutdown()` sí se ejecutó).
- **Catálogo de eventos del contrato (`CONTRACT_EVENTS`, en `registry.py`):** mapea los 15 nombres de `docs/contracts/IPlugin.md` ("Eventos disponibles") contra clases reales de `BaseEvent`. **Solo 4 de 15 existen hoy como evento real:** `KERNEL_READY`→`KernelInitializedEvent`, `KERNEL_SHUTDOWN`→`KernelShutdownEvent` (equivalencias semánticas ya documentadas), y los **2 nuevos de esta tarea** `PLUGIN_LOADED`→`PluginLoadedEvent`, `PLUGIN_UNLOADED`→`PluginUnloadedEvent` (`src/aries/plugins/events.py` — viven junto al código de plugins, no en `core/events.py`, corrigiendo hacia adelante la inconsistencia de ubicación que el diagnóstico del 24 ya había señalado, sin tocar los 2 eventos existentes). **Los otros 11** (`KERNEL_STARTING`, `INTENT_DETECTED`, `PLAN_*`, `ACTION_*`, `MEMORY_*`, `ERROR_OCCURRED`) siguen bloqueados por Planner/Agent-Tool/Memory-EventBus, exactamente como ya documentaba `docs/audits/2026-07-24-diagnostico.md`. Un plugin puede declarar un hook para cualquiera de los 15 nombres del contrato sin que `PluginRegistry` lo rechace — los 11 sin clase real quedan registrados en `LoadedPlugin.unavailable_hooks` con una advertencia en el log, en vez de fallar silenciosamente o simular una conexión que nunca va a disparar nada.
- **`src/aries/plugins/installer.py`**: `check_requirements(metadata, loaded_plugin_names)` verifica cada entrada de `requires` contra plugins ya cargados **y** contra paquetes pip importables (`importlib.util.find_spec`, sin importar el paquete de verdad) — una entrada puede ser cualquiera de los dos, no hay forma de saber cuál a priori así que se comprueban ambas. `missing_requirements()` es el atajo con solo lo que falta. **`install_requirements()` es un stub que SIEMPRE levanta `NotImplementedError`**, con docstring explicando que es una decisión de seguridad explícita de esta tarea (instalar paquetes declarados por un plugin de terceros sin supervisión es ejecución de código arbitrario) — no se activó, no se rodeó, no se llama desde ningún otro lugar del código nuevo.
- **`PluginRegistry.load()` bloquea el load si falta un requirement** (no solo advierte) — decisión tomada sin bloquear esta noche, la más conservadora entre "bloquear" y "advertir y dejar que `initialize()` decida": evita dejar activo un plugin con una dependencia declarada como faltante.
- **Plugin de ejemplo real:** `tests/fixtures/example_plugin/` (`manifest.json` + `plugin.py`, `ExamplePlugin(IPlugin)` completo) — usado por todos los tests de integración de `PluginRegistry`, cargado de disco de verdad vía `importlib`, nunca mockeado. Declara un hook para `KERNEL_READY` (real) y otro para `INTENT_DETECTED` (inexistente, a propósito, para probar `unavailable_hooks`).
- 62 tests nuevos: `test_plugin_manifest.py` (24), `test_plugin_loader.py` (8), `test_plugin_installer.py` (10), `test_plugin_registry.py` (20, integración real — plugin real cargado, `initialize()` real, evento real (`KernelInitializedEvent`) publicado y capturado por el hook real del plugin, `shutdown()` real al descargar, hooks desconectados de verdad tras `unload()` verificado con un segundo `publish()`, y los modos de falla: manifest/módulo/clase inválidos, requirement faltante, `initialize()` que retorna `False` o lanza, `register_hooks()` que lanza con rollback verificado).
- **Hallazgo no relacionado, documentado arriba en su propia sección:** `events/event_bus.py` tiene una regresión (clase `EventBus` local duplicada) que no causé ni corregí — ver "Hallazgo sin resolver" al inicio de este archivo.

## Qué NO existe todavía (pendiente real)
- Existen cuatro `IAgent` concretos (`FileSystemAgent`, `ProcessAgent`, `GitAgent`, `DatabaseAgent`) y ahora `AgentManager` los conecta (registro + ruteo por nombre). El resto de los agentes documentados en `docs/contracts/IAgent.md` (WindowsAgent, DockerAgent, EmailAgent, BrowserAgent, HomeAssistantAgent) sigue sin implementar. `agents/base.py` sigue vacío. Nada en `core/kernel.py` invoca todavía a `AgentManager` — el Kernel sigue sin construirlo ni usarlo (siguen sin cablear al flujo real, igual que Memory antes de la tarea que la conectó). Tampoco hay descubrimiento automático de agentes vía `plugins/` (deliberadamente fuera de alcance de `AgentManager`).
- `src/aries/plugins/` está implementado (manifest, loader, registry, context, installer sin instalación real) pero **ningún plugin real de terceros existe todavía** — solo el plugin de ejemplo de tests. **Deliberadamente sin implementar:** `install_requirements()` (instalación real de paquetes pip declarados por un plugin) es un stub que siempre lanza `NotImplementedError` — decisión de seguridad explícita, no pendiente por pereza. Tampoco hay descubrimiento automático de plugins en disco (nadie escanea un directorio de plugins al arrancar) ni conexión de `PluginRegistry` a `core/kernel.py` — el Kernel no construye ni usa `PluginRegistry` todavía. 11 de los 15 eventos que `docs/contracts/IPlugin.md` espera (`KERNEL_STARTING`, `INTENT_DETECTED`, `PLAN_*`, `ACTION_*`, `MEMORY_*`, `ERROR_OCCURRED`) siguen sin existir como `BaseEvent` real — un plugin puede declarar un hook para ellos pero nunca va a dispararse.
- No hay ninguna clase concreta que implemente `ITool`.
- `src/aries/core/kernel.py` no integra memoria, agentes, plugins ni planner reales, solo usa sleeps como stub.
- No hay persistencia de memoria.
- No hay planner ni ejecución de tools definidos en el código.
- No hay implementación de subsistema de voz.
- `src/aries/events/`: existe implementación de Event Bus con tests (motor sólido, ya revisado en `docs/audits/2026-07-24-diagnostico.md`, con la regresión de `event_bus.py` documentada arriba), y ahora define 4 eventos de dominio reales (`KernelInitializedEvent`, `KernelShutdownEvent`, `PluginLoadedEvent`, `PluginUnloadedEvent`) de los 15 que `docs/contracts/IPlugin.md` da por hechos — quedan 11 sin implementar (`KERNEL_STARTING`, `INTENT_DETECTED`, `PLAN_*`, `ACTION_*`, `MEMORY_*`, `ERROR_OCCURRED`), todos bloqueados por Planner/Agent-Tool/Memory-EventBus, no por `plugins/`.
- `tests/unit/test_kernel.py::test_kernel_publishes_initialized_event` y `::test_kernel_publishes_shutdown_event` son preexistentes y **flaky** (no causado por el trabajo de hoy, y se hizo visible recién ahora porque antes ni siquiera se podía correr `pytest` por el import circular): comparan un `BaseEvent` publicado por el kernel contra una instancia nueva creada en el test, por igualdad. Como `BaseEvent.timestamp` se genera con `datetime.now(UTC)` en cada instancia, la igualdad depende de que ambos timestamps coincidan al microsegundo — casi nunca se cumple, y cuál de los dos tests falla varía de corrida en corrida. Pendiente de decidir (excluir `timestamp` de la igualdad de `BaseEvent`, o cambiar el test para comparar solo `event_type`) — fuera de alcance de la tarea de `memory/` de hoy.
- `src/aries/container/`: eliminado, no forma parte del path de ejecución actual.

## Próximo paso recomendado
Implementar los eventos de dominio restantes (`INTENT_DETECTED`, `PLAN_CREATED`, `PLAN_EXECUTED`, `ACTION_*`, `MEMORY_*`, `ERROR_OCCURRED`) que exige `docs/contracts/IPlugin.md` — ya no bloquean a `plugins/` (que quedó implementado esta noche con los 4 eventos que sí existían), pero siguen bloqueando a Planner y a la integración de `memory/` con el Event Bus.

## Reglas para mantener este archivo
- Actualizar la tabla y "Qué existe implementado" al cerrar cada tarea, una línea por módulo
- Nunca borrar fases completadas, solo agregar filas nuevas
- "Próximo paso recomendado" siempre debe tener una sola tarea, nunca varias opciones

---

## Resumen de la sesión nocturna autónoma (2026-07-24, sin supervisión)

Instrucción de la sesión: trabajar las 3 tareas en orden, no bloquear con `AskUserQuestion` ante ambigüedad genuina (elegir la opción más conservadora y documentar), avanzar solo si la suite pasa dentro del baseline conocido.

**Tarea 1 (principal) — GitAgent: completa.** Tercer `IAgent` concreto (`src/aries/agents/git/agent.py`), 49 tests con repos git reales (sin mockear `git`), sin tocar Planner/`plugins/`/otros agentes. Detalle completo arriba en "Qué existe implementado". Suite verde dentro del baseline al terminar.

**Tarea 2 (si sobraba tiempo) — Planner.spec.md: completa.** Borrador en `docs/specs/Planner.spec.md`, marcado "BORRADOR — requiere aprobación", sin una sola línea de código de Planner. 9 puntos `[REQUIERE DECISIÓN]` explícitos sin resolver — ver el archivo.

**Tarea 3 (si sobraba tiempo) — salud general: completa, con un hallazgo que no estaba en el guion.**

- Suite completa corrida dos veces: 164 tests recolectados, 163 passed, 1 failed en ambas corridas — siempre `test_kernel_publishes_shutdown_event`, uno de los 2 flaky ya documentados en "Baseline conocido". Ningún test nuevo falló, ningún test distinto al baseline falló. Nada que documentar como regresión.
- **Hallazgo no solicitado, importante:** al revisar `git status` para este resumen, el repo aparece **2 commits adelante de `origin/master`** que yo no creé mediante ninguna llamada a `git add`/`git commit` en esta conversación (no tengo ningún tool call de ese tipo en el historial de esta sesión). Son:
  - `adb9429` "Completa refactor de Event Bus (IEventBus, AsyncEventBus, call sites)"
  - `2fb87be` "memory/: expires_at + clear_expired; FileSystemAgent y ProcessAgent"

  Ambos con autor `oropezadev-wq <oropeza.dev@gmail.com>` (coincide con `git config user.name/email` de este repo) y timestamp `2026-07-24 23:17`. El **contenido** de ambos commits coincide exactamente con trabajo legítimo de tareas anteriores de esta misma sesión (el mismo que ya está descrito arriba en este archivo) — no hay nada roto, perdido, ni código ajeno. No encontré hooks de git customizados (`.git/hooks/` solo tiene los `.sample` por defecto) que expliquen el mecanismo. Dado el contexto ("VSCode native extension environment"), sospecho que fue algún mecanismo de checkpoint/snapshot de la extensión o del entorno, no un comando `git commit` mío — pero no puedo confirmarlo con certeza desde acá.
  
  **No tomé ninguna acción sobre esto** (no hice `reset`, no hice `push`, no intenté deshacerlo) — solo lo dejo documentado para que se revise mañana, siguiendo la instrucción de la sesión de no bloquear y documentar en vez de decidir. Dado que la regla del proyecto es "nunca comitear salvo que se pida explícitamente", esto vale la pena confirmarlo mañana aunque el contenido en sí no tenga nada de malo.

### Alcance respetado
No se tocó `plugins/`, no se implementó código de Planner, no se tocaron agentes fuera de `GitAgent` (`FileSystemAgent`/`ProcessAgent` quedaron intactos — verificable en que sus tests siguen pasando sin cambios).

---

## Resumen de la sesión nocturna autónoma (2026-07-24, segunda noche, sin supervisión)

Instrucción de la noche: nunca `git commit`/`git push` bajo ninguna circunstancia; ante ambigüedad, opción más conservadora documentada acá, sin bloquear.

**Tarea 0 (primero) — investigación de los 2 commits: completa.** Ver sección al inicio de este archivo ("Investigación de los 2 commits inesperados"). Conclusión: contenido 100% coincide con trabajo ya documentado en ambos commits, línea por línea contra `git show --stat`; no se marcó `[SIN RESOLVER]` porque la condición que la propia tarea definió para ese flag (contenido sin coincidencia) no se cumplió — sí queda sin confirmar el *mecanismo* de cómo se comitearon, y eso se dejó visible al inicio del archivo, no enterrado.

**Tarea 1 (principal) — DatabaseAgent: completa.** Cuarto `IAgent` concreto (`src/aries/agents/database/agent.py`), usando SQLAlchemy Core sobre SQLite. Antes de escribir código, encontré y resolví un bloqueador de entorno no anticipado: `sqlalchemy` estaba declarada en `pyproject.toml` pero no instalada en el entorno real de este proyecto (Python global — confirmé que `.venv`/`.venv-1` no son el entorno en uso); la instalé porque ya era una dependencia aprobada, no una nueva. Verifiqué empíricamente los 3 vectores de inyección SQL relevantes (params, nombres de columna vía `values`/`where`, valores con sintaxis SQL arbitraria) antes de escribir el agente, mismo criterio que `shell=True` en `ProcessAgent`. 48 tests contra SQLite real, incluyendo `TestSQLInjection` dedicada que verifica el estado real de la tabla después de cada intento, no solo el `ActionResult`. Detalle completo arriba en "Qué existe implementado". No toqué Planner, `plugins/`, ni otros agentes.

**Tarea 2 (si sobraba tiempo) — salud general: completa.** Suite completa corrida dos veces tras terminar DatabaseAgent: 212 tests recolectados, 211 passed / 1 failed en ambas corridas — siempre dentro del baseline conocido (`test_kernel_publishes_shutdown_event` una vez, `test_kernel_publishes_initialized_event` había aparecido en una corrida anterior de la misma noche durante el desarrollo de DatabaseAgent — ambos ya documentados, ninguno nuevo). Ningún test distinto al baseline falló en ningún momento de la noche. Nada que documentar como regresión.

**Git:** ni `git add` ni `git commit` ni `git push` en ningún momento de esta sesión, por instrucción explícita. El working directory queda con todos los cambios sin comitear, como se pidió.

### Alcance respetado (segunda noche)
No se tocó `plugins/`, no se implementó código de Planner, no se tocaron `FileSystemAgent`/`ProcessAgent`/`GitAgent` (los tres siguen pasando sus tests sin cambios).
